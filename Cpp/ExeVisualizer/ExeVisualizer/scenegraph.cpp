/* ----------------------------------------------------------------
   name:           scenegraph.c
   purpose:        implementation of the scenegraph-functions
   version:		   SKELETON CODE
   author:         andy nealen
				   computer graphics
				   tu berlin
   ------------------------------------------------------------- */

#include <stdlib.h>
#include "scenegraph.h"

#define RES 20 // bestimmt die Kantigkeit von Kugeln und Zylindern

bool showAxes = true;

GLfloat mat_teapot_shininess[] = { 128.0 };
GLfloat mat_shininess[] = { 3.0 };

// traverse the scenegraph from a given node
void sceneGraphTraversal(Node *node)
{

	

		// apply local transformation
	//	transformNode(node);
		// draw the current node
		

		
		//drawObj(node);

}
