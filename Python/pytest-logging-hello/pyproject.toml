# Python-related project configuration.

[project]
name = "pytest-logging-hello"
version = "0.1.0"
dependencies = [
    "pytest",
    "requests",
]

[tool.pytest.ini_options]
log_cli = true
log_cli_level = "DEBUG"
log_cli_format = "%(asctime)s [%(levelname)s]: %(filename)s:%(lineno)d: %(message)s"
log_cli_date_format = "%Y-%m-%d %H:%M:%S"

log_file = "pytest.log"
log_file_level = "DEBUG"
log_file_format = "%(asctime)s [%(levelname)s]: %(filename)s:%(funcName)s:%(lineno)d: %(message)s"
log_file_date_format = "%Y-%m-%d %H:%M:%S"
