[project]
name = "exceptiongroups-py3.10-backport-hello"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "bpython",
    "exceptiongroup",
]

[tool.hatch.build.targets.wheel]
packages = ["main.py"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
