[project]
name = "synthesizer-hello"
version = "0.1.0"
description = "A lil showcase project for the 3rd party lib`synthesizer`"
readme = "README.md"
requires-python = ">=3.14.3"
dependencies = [
    "synthesizer",
    "pynput",
    "bpython",
    "pyaudio>=0.2.14",
]
