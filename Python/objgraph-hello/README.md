# Objgraph Hello

Visualize and track Python object graphs using the `objgraph` library.

## Setup

Install system dependencies:

```bash
sudo apt install --yes libcairo2-dev libgirepository1.0-dev
```

Install Python dependencies and set up the environment:

```bash
uv sync
```
